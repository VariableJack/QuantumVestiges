package com.gamerparadise.component;import org.springframework.stereotype.Component;import org.springframework.beans.factory.annotation.Autowired;import org.apache.logging.log4j.LogManager;import org.apache.logging.log4j.Logger;import lombok.NonNull;import java.util.List;import com.gamerparadise.builder.CartBuilder;import com.gamerparadise.builder.GamesBuilder;import com.gamerparadise.component.dto.UpdateCartComponentInputDTO;import com.gamerparadise.component.dto.CartComponentDTO;import com.gamerparadise.component.dto.GameComponentDTO;@Componentpublic class CartComponent {	@Autowired	private CartBuilder cartBuilder;	@Autowired	private GamesBuilder gamesBuilder;	private static final Logger logger = LogManager.getLogger(CartComponent.class);		public List<CartComponentDTO> getCart(@NonNull String username) {		return cartBuilder.getCart(username);	}	public void updateCart(@NonNull UpdateCartComponentInputDTO input, @NonNull String username) {		final List<CartComponentDTO> existingCart = this.getCart(username);		GameComponentDTO game;		CartComponentDTO cartItem;		switch(input.getAction()) {			case "add":				if (existingCart.stream().filter((existingCartItem) -> existingCartItem.getGameId().equals(input.getGameId())).count() > 0) {					throw new RuntimeException("Error: cannot add the same item to your cart");				}				game = gamesBuilder.getGameById(input.getGameId());				logger.info("Adding {} to {} cart", game, username);				cartItem = CartComponentDTO.builder()					.username(username)					.gameId(game.getGameId())					.gameName(game.getGameName())					.franchiseId(game.getFranchiseId())					.franchiseName(game.getFranchiseName())					.build();								try {					// cartBuilder.insertItem(cartItem)				} catch (Exception e) {					//console.log(e)					//return {					//	statusCode: 400,					//	headers: { 'Access-Control-Allow-Origin': 'https://localhost:3000', 'Access-Control-Allow-Credentials': 'true', 'Content-Type': 'application/json' },					//	body: JSON.stringify({					//		message: 'Failed to add the game to your cart. Please try again. If this error persists, please cut us a support ticket'					//	})					//}				}				break;			case "remove":				if (existingCart.stream().filter((existingCartItem) -> existingCartItem.getGameId().equals(input.getGameId())).count() == 0) {					throw new RuntimeException("Error: cannot remove this item from your cart");				}				game = gamesBuilder.getGameById(input.getGameId());				cartItem = CartComponentDTO.builder()					.username(username)					.gameId(game.getGameId())					.build();								try {					// cartBuilder.removeItem(cartItem)				} catch (Exception e) {					//console.log(e)					//return {					//	statusCode: 400,					//	headers: { 'Access-Control-Allow-Origin': 'https://localhost:3000', 'Access-Control-Allow-Credentials': 'true', 'Content-Type': 'application/json' },					//	body: JSON.stringify({					//		message: 'Failed to remove the game from your cart. Please try again. If this error persists, please cut us a support ticket'					//	})					//}				}			default:				throw new RuntimeException("Bad request - action not supported");		}	}	public void checkoutCart(@NonNull String username) {		final List<CartComponentDTO> existingCart = this.getCart(username);		//purchasedGamesBuilder.addGames(existingCart, username);		cartBuilder.clearCart(username);	}}